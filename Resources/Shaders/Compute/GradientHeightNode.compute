#pragma kernel CSMain

Texture2D<float> _RampTexture;

RWTexture2D<float> _OutTexture;

[numthreads(8, 8, 1)]
void CSMain(uint3 id : SV_DispatchThreadID)
{
    uint2 uv = id.xy;
    
    uint2 rampUV = uint2(uv.x, 0);
    float height = _RampTexture[rampUV];
    
    _OutTexture[uv].r = height;
}
